# SQL Server (Compose imposta DB_SERVER=sqlserver)
# Evita $ ! % nella password: Compose e bash le interpretano.
# Se usi $ in Compose, scrivilo doppio: $$
MSSQL_SA_PASSWORD=ChangeMe_StrongP1
DB_USER=sa
DB_PASSWORD=ChangeMe_StrongP1
DB_NAME=GESTIONALE_AUTO

# Mailer
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
